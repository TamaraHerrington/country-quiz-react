const HintZero = (props) => {
    return (
        props.hintVisible ? 
        <image src= {country.flags.png}/>
        :
        <> </>
    )
}

export default HintZero;

//return props.hintVisible ? <img /> : <></>
